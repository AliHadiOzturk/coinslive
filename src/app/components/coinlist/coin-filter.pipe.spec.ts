import { CoinFilterPipe } from './coin-filter.pipe';

describe('CoinFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CoinFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
